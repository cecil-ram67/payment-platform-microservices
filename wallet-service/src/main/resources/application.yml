server:
  port: 8083

spring:
  application:
    name: wallet-service
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: wallet-group

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
